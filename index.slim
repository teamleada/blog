---
layout: default
title: The Leada Blog - Company News, Tips, and Features
---

.container.full-container.card-container
  | {% for post in site.posts %}
  .row.card
    | {% assign author = site.leadas[post.author] %}
    .col-md-2
      .author-info
        img src="/assets/images/leadas/{{ author.photo }}" class="author-image"
        h4 {{ author.name }}
      a href="{{ post.url }}" {{ post.date | date_to_string }}<br>
      a href="{{ post.url }}#comment" Leave a comment
    .col-md-10
      h1
        a href="{{ post.url }}" {{ post.title }}
      .post-content
        | {{ post.content }}
  | {% endfor %}
